# سجل تغييرات n8n — «My workflow» (TrAB9AObRMVX6Gjc)

هذا الملف سجل نصي للنسخ المنشورة من الـworkflow، لأن الـworkflow نفسه يعيش في n8n Cloud
(نسخته الكاملة محفوظة في version history داخل n8n وفي نسخة مؤرشفة باسم
`BACKUP My workflow 2026-09-10 قبل إصلاح الشبح والتأكيد` = uiCIE8g2Fnr8rc2U).

## 2026-09-11

### 434fc59d — «الفحص الثاني المركّز: كود واحد، سؤال واحد، أعمى، ومرة واحدة»
الخيار (ب) كما طلبت صاحبة النظام: الصف يبقى 🔍 حتى يحسمه سؤال واحد في التشغيل التالي.

- **من أين يأتي الكود المعلّق**: من صفوف الطلبات نفسها، لا من حالة جديدة في الشيت. «فحص السجل»
  يقرأ تبويب الطلبات (وهو يقرأه أصلاً) ويلتقط كل صف **🔍** سببه نقص عدد عبوات لكود R
  (`R__: الفاتورة …جم = N عبوة من …جم ولم يظهر منها إلا M`)، فيصير ذلك الكود سؤال التشغيل التالي.
  صف ✅ لا يُسأل عنه، وصف ⚠️ بفرق عدد قطع (كـ`qwd-5-1315`) ليس نقص عبوات فلا يُسأل عنه.
- **السؤال أعمى**: `count_code` على `/verify-order` يرسل سؤالاً واحداً —
  «كم عبوة منفصلة تحمل الكود R__ تظهر معاً في نفس الإطار؟» — بلا فاتورة، وبلا عدد متوقّع،
  وبلا أي تلميح إلى وجود خلاف أصلاً، مع الأمر بتجاهل كل كود آخر ومنع الجمع عبر الإطارات.
- **لقطات ذلك الكود فقط**: `count_frames` تأتي من `label_sightings` المحفوظة للقراءة الأولى
  (مع الإطار السابق واللاحق لكل لقطة)، فلا يُعاد إرسال الفيديو كله.
- **ولا يُشترى مرتين**: العلامة تُخزَّن في عمود «حالة الملف» (G) تحت `recheck`،
  وكل كود يُسأل **مرة واحدة في عمر الملف**: `done:N` إذا حُسم، و`done:unclear` إذا لم يُحسم
  (الصف يبقى 🔍 بلا إعادة شراء)، و`fail:1` ثم `done:failed` إذا تعذّر الاستدعاء مرتين.
  «أعد المعالجة» وحده يمسح كل ذلك.
- **لا تُمسّ القراءة المحفوظة**: الرد الواضح (ثقة high أو medium وعدد > 0) يُرقِّع `seen_count`
  للكود داخل نسخة من القراءة المخزّنة ويُعاد كتابتها كقراءة سليمة، فيُعاد بناء الصف بالقواعد
  نفسها. وفشل الاستدعاء المركّز **لا** يكتب «فشل القراءة» ولا يمسح شيئاً.
- تحقّق قبل النشر: تشغيل 845 على نسخة مؤقتة بالكتابة معطّلة — **صفر استدعاء API**
  ومخرج مطابق حرفياً (115 صفاً، صفر فرق) لتشغيل 844، لأن لا صف 🔍 بنقص عبوات موجود الآن.
  منطق الفحص نفسه مغطّى بـ22 تأكيداً في `n8n/rules.test.js` (المجموعتان 23 و24) و13 تأكيداً
  في `tests/test_line_rules.py` (المجموعة 22).
- على جانب الخدمة: كوميت `95b7939` (`count_code`/`count_frames`/`count_code_only`) و`59bfbb1`
  (الاختبارات) — والقراءة العادية لا تتغيّر: `count_code` معطّل افتراضياً.

### a2e5aef8 — «العدّ الأعمى: علبتان في الفيديو تُقرآن علبتين»
الأولوية التي طلبتها صاحبة النظام: الإصلاح الحقيقي في **القراءة** لا في شبكة الأمان.

- **تراجع عن قاعدة `4df0e123`**: «الفاتورة عبوتان وظهرت واحدة» لم تعد تُبتلع ولا تصبح ✅ أبدًا.
  `judgePrep` صار يعيد `needs_review` في هذه الحالة ⇒ الصف **🔍 يحتاج مراجعتي** مع السبب صريحًا
  (`R__: الفاتورة 500جم = 2 عبوة من 250جم ولم يظهر منها إلا 1`). القاعدة السابقة كانت تُخرج ✅
  على نقص حقيقي، وهذا ممنوع: شبكة الأمان توقف الصف، لا تُمرّره.
- **`blind_count: true` في «HTTP Request»**: الخدمة صارت تُرسل للنموذج بروتوكول عدّ مُلزِمًا،
  وتحجب عنه الفاتورة والعدد المتوقع حتى تبقى القراءة عمياء فلا يخترع عبوة:
  1. لكل إطار يُسرد كل ملصق ظاهر مع موضعه (أعلى/أسفل/يمين/يسار).
  2. عدد العبوات لكل كود = **أكبر عدد ملصقات منفصلة لنفس الكود ظهرت معًا في إطار واحد**،
     لا مجموع الإطارات (وإلا تكرّرت العبوة الواحدة بعدد الإطارات).
  3. ملصقان متطابقان في موضعين مختلفين من نفس الإطار = عبوتان، ولو تطابق كل شيء فيهما.
  4. كل إطار يصل النموذج مسبوقًا بنصّ «إطار N — الثانية T.TTT» ليُميّز الإطارات، بدقة أصلية
     1080×1920 بلا تصغير.
- **ملاحظة إيجابية بدل الصمت**: عندما يتطابق الوزن والعدد ≥ 2 يُكتب في السبب
  `R75: عبوتان × 250جم = 500جم — مطابق للفاتورة وسماك` — فيُرى في الشيت أن العدّ حصل فعلًا.
- **✅ مع سبب يقول إن التحقق ناقص: ممنوعة**. عبارة «التغطية جزئية — تحقق غير مكتمل» صارت
  تُرجَع من `judgePrep` منفصلة في `covNote`، ولا تُضاف إلا على الصفوف التي **لا** تخرج ✅.
- تحقّق نهائي على بيانات 2026-09-10 الحقيقية في نسخة مؤقتة بالكتابة معطّلة (تشغيل 844، صفر API):
  `qwd-5-1318` ← ✅ بالسبب المطلوب حرفيًا · `qwd-5-1315` ← ⚠️ كما هي (صابونة واحدة لا تُعدّ أربعًا) ·
  `qwd-5-1314` ← ✅ · عدد صفوف ✅ التي تقول إن التحقق ناقص = **0** · لا صف آخر تغيّر.
- الاختبارات: `n8n/rules.test.js` صار **79 تأكيدًا** — المجموعة 20 تختبر الاتجاهين (العدّ الصحيح
  ⇒ ✅ بلا ملاحظة وزن، والنقص ⇒ 🔍 لا ✅ ولا 📦)، والمجموعة 21 تثبّت `qwd-5-1315` ⚠️،
  والمجموعة 22 تثبّت أن القاعدة ضيّقة (750جم ⇒ 🔍).
- على جانب الخدمة (Render): `blind_count` في `VerifyRequest` و`_COUNT_PROTOCOL` و`label_sightings`
  و`_frame_index` (كوميت `85b46db`)، ثم قراءة الوزن المطبوع داخل اسم السطر نفسه عند ترك النموذج
  حقل الوزن فارغًا — يُقرأ ولا يُخترع ولا يتجاوز وزنًا صريحًا (كوميت `12d473b`، اختبارات `7ae50fc`).

### 4df0e123 — «وزن الملصق وزن عبوة واحدة: المضاعف الصحيح ملاحظة لا مخالفة»
- «Build Orders Table» / `judgePrep`: كان يقارن **مجموع** وزن ما ظهر بمجموع الوزن المفوتر،
  فصار `qwd-5-1318` بحالة 📦 بسبب `R75`: الفاتورة 500جم (سطران × 0.25 كيلو في سماك) والملصق
  الظاهر 0.250 كيلو و`seen_count` واحد. الآن: إن كان الوزن المفوتر **مضاعفاً صحيحاً ≥ 2**
  لوزن العبوة الواحدة والمفوتر أكبر من الظاهر، فالفرق في **عدد العبوات** لا في الوزن،
  وعدّ العبوات بصريًا أضعف من أن يُتّهم به التحضير ⇒ يُنقل من «مخالفة» إلى
  «لم أتمكن من التحقق من: R75: الفاتورة 500جم = 2 عبوة من 250جم ولم يظهر منها إلا 1».
- القاعدة ضيّقة عمداً: 300جم مقابل عبوة 250جم تبقى مخالفة · محضَّر أكثر من المفوتر يبقى مخالفة ·
  صنف مفوتر لم يُحضَّر يبقى مخالفة.
- **لا تمسّ `qwd-5-1315`**: ⚠️ هناك تأتي من مقارنة الفيديو بسماك (عدد القطع 4 مقابل 1)
  لا من `judgePrep`، وفاتورتها بالحبة بلا وزن فلا يعمل فرع الوزن أصلاً.
- تحقّق على البيانات الحقيقية بصفر استدعاء API: `qwd-5-1318` ← ✅ · `qwd-5-1315` ← ⚠️ كما هي ·
  📦 من 1 إلى 0 · ✅ من 69 إلى 70 · وبقية التوزيع كما هو.

### 787dc3ca — «مَن وُسم نسخة مكررة لا يُقرأ مرة أخرى»
- «فحص السجل»: إذا كان سجل الملف يحمل `skip: "duplicate"` فهو يبقى نسخة مكررة في كل تشغيل
  (لا API ولا صف) إلا عند «أعد المعالجة» — بدون هذه القاعدة كانت علامة التكرار المخزَّنة
  تُقرأ كـ«لا نتيجة صالحة» فيُعاد شراء القراءة.
- تنظيف أثر ما قبل الإصلاح: ثلاث تسجيلات رُفعت مرتين بمعرّفات Drive جديدة
  (023329 · 015934 · 012456) فأنتجت ستة صفوف لثلاثة طلبات، وأحدها التقط فاتورة غريبة
  (qwf-5-1107 بتاريخ 09-08). وُسمت صفوف السجل 99–101 نسخاً مكررة، فتسقط صفوفها في التشغيل
  المجدول التالي وتعود الفاتورة لصفها الصحيح.

### 3a4468b2 — «صمود الدفعة: ملف واحد لا يُسقطها، ولا شراء قراءة مرتين»
سببا سقوط التشغيلين 804 و806 (2026-09-11): واجهة Anthropic أعادت **502 Bad Gateway**
فأعادت مكتبة anthropic المحاولة داخليًا وامتدت القراءة الواحدة إلى 10–25 دقيقة
(الطلب الذي تخلّى عنه 806 في 09:08:52 نجح فعلًا في 09:27:57)، ومهلة عقدة «HTTP Request»
120 ثانية × 3 محاولات ثم **إسقاط التشغيل كله** لأن onError غير مضبوط. لا علاقة للذاكرة:
لا OOM ولا إعادة تشغيل، والذروة 22٪ من 2 جيجابايت (الخطة 1c-2g، ولم تُلمس).
- «HTTP Request»: `onError: continueRegularOutput` + مهلة 480 ثانية + إلغاء إعادة محاولة n8n
  (إعادة المحاولة صارت في الخدمة: محاولة واحدة بعد 60 ثانية).
- «فحص السجل»: القرار أُعيد بناؤه —
  **(4)** القراءة الناجحة المحفوظة بمعرّف الملف تُستخدم في كل التشغيلات اللاحقة، كُتب صف أم لا
  (حُذف شرط `matched[id]` الذي كان يشتري القراءة ثانية عند كل سقوط، وحُذف `badOc`: حقل ناقص
  لا يفرض إعادة قراءة). إعادة القراءة فقط عند «أعد المعالجة».
  **(5)** نسخة مكررة: تطابق `md5Checksum` (أو الاسم + الحجم) لملف مقروء سابقًا ⇒ لا API ولا صف،
  والسبب «نسخة مكررة من <الملف>».
  **(3)** ملف أسقط الخدمة في تشغيلين منفصلين ⇒ لا يُقرأ، والسبب «الملف يُسقط الخدمة».
- «Parse Video Result»: مسار فشل/تخطٍّ لا يُفسد القراءة المحفوظة — `no_row` وسبب صريح،
  وأخطاء الاتصال (connection/ECONNRESET/timeout/502/503/504) لا تُحسب محاولة وترفع عدّاد
  «إسقاط الخدمة» بدلًا من ذلك.
- «كتابة سجل الاستخراج»: المدى A:G وعمود **G «حالة الملف»** = `{size, md5, crashes, note}`.
- «تحديد الملفات المقفلة»: تُضاف الملفات التي أسقطت الخدمة مرتين.
- «Build Orders Table»: عناصر `no_row` (مكررة أو فاشلة القراءة) لا تُنتج صفًا.
- **التنبيه**: workflow «تنبيه فشل التشغيل» كان `active: false` فلم يُستدعَ في أي سقوط
  (صفر تنفيذات في تاريخه). نُشِّط ونُشر، واختبار حقيقي مجدول فشل عمدًا في 10:50 بتوقيت الرياض
  ⇒ التنفيذ 818 نجح وكتب صف التنبيه في `الطلبات!A117`، ومسار تحرير القفل عمل (0 صفوف قفل
  لأن الاختبار لم يأخذ قفلًا).
- منطقة n8n الزمنية **Asia/Riyadh**: أي cron يُكتب بتوقيت الرياض لا UTC.

## 2026-09-10

### dc75efb0 — «قاعدة تاريخ الطلب + عمود مصدر التاريخ»
هرمية التاريخ المعتمد (قاعدة صاحبة النظام): المطبوع في الفاتورة ← تاريخ فاتورة سماك
المطابَقة ← يوم رفع الملف إلى Drive ناقص يوم ← يوم أول قراءة للملف ناقص يوم.
- **(ب) تاريخ اسم الملف ملغى بالدليل**: على 51 ملفًا تاريخها المطبوع معروف طابق اسم الملف
  في 21 فقط وخالف في 30 (27 بفارق 10 أيام، 2 بفارق 30، 1 بفارق 6) — فاسم الملف تاريخ
  إعادة الإرسال لا تاريخ الطلب، ولا يُستخدم مصدرًا.
- «Build Orders Table»: `DSRC_PRINTED/DSRC_INV/DSRC_UPLOAD/DSRC_READ` و`dayMinus1` بتوقيت
  الرياض، و`DRIVE_CREATED` من «Search files and folders» (أُضيف `createdTime` إلى fields)،
  و`META.readAt` و`META.odate`.
- الاحتياط (ج) يُطبَّق **بعد المطابقة وحدها**، والتاريخ التقديري لا يدخل `strict` ولا
  `dateCheck` — فلا يقيّد مطابقة ولا يردّها.
- تاريخ موروث من الشيت بلا سند (لا مطبوع ولا فاتورة مطابَقة) يُعاد حسابه بالقاعدة (ج).
- 🚨 مع تاريخ تقديري: `samakWindowLoaded` تشترط تقارير الأيام الثلاثة (قبل/نفس/بعد) كلها؛
  وإلا «بانتظار تقرير سماك ليوم YYYY-MM-DD». التاريخ المطبوع يبقى على القاعدة السابقة
  (تقرير يومه ±1). السبب يذكر «تاريخ تقديري (المصدر)».
- «فحص ذاتي للصف»: عمود Q «مصدر التاريخ» في الترويسة وفي كل صف.
- الأثر على الشيت: 98 صفًا كلها لها تاريخ ومصدر (56 مطبوع · 32 من فاتورة سماك · 10 يوم
  القراءة −1)، والتوزيع 62 ✅ / 0 ⚠️ / 13 🔍 / 20 🔵 / 3 🚨 — صفران انتقلا من 🚨 إلى
  «بانتظار تقرير سماك» لأن تاريخهما تقديري.

### a152f26d — «حمل تاريخ الطلب عند إعادة استخراج لا تقرأ تاريخًا»
- «Build Orders Table»: عند استبدال صف بملف أُعيد استخراجه صار التاريخ
  `v.order_date || carried.date || ''` بدل `v.order_date || ''`.
  السبب الجذري لفراغ عمود التاريخ في qwf-5-1106 و qws-5-5260: الاستخراج أعاد
  `order_date: null` (التاريخ مطبوع على الفاتورة لكن المخطط لم يكن يوجّه لقراءة سطر
  «Printed At»)، وحمل جانب سماك كان ينقل الفاتورة والمنصة والفرع والإجمالي وطريقة الدفع
  ولا ينقل التاريخ — فيُمسح في أول تشغيل تالٍ.
- الجانب الآخر من الإصلاح في الخدمة: كوميت app/main.py — قراءة التاريخ من
  «Printed At / Received at / تاريخ الطباعة» وتطبيعه بصيغة اليوم أولًا إلى YYYY-MM-DD.

### 2c21dbf0 — «🚨 فقط مع تقرير يغطي التاريخ + أخطاء API لا تُحسب محاولة + تجديد القفل»
- «Build Orders Table»: `SAMAK_DAYS` تُبنى من تواريخ تقارير سماك المحمَّلة في التشغيل، و
  `samakDayLoaded(date)` تقبل التاريخ ±1 يوم. لا تُعلن الحالة «🚨 طلب بلا فاتورة سماك» إلا إذا
  كان تقرير يوم الطلب محمَّلًا فعلًا؛ وإلا فالحالة «يحتاج مراجعتي» والسبب
  «بانتظار تقرير سماك ليوم YYYY-MM-DD». الطلب بلا تاريخ مقروء يبقى كما كان.
- «Parse Video Result»: عدّاد المحاولات لا يزيد إذا كان رد الخدمة نصًّا يدل على خطأ منصة
  (`credit balance` / `rate_limit` / `overloaded` / `api_error` / `insufficient_quota` /
  `too many requests`). أما الخطأ الصريح 5xx فهو أصلًا لا يزيد العدّاد لأن التشغيل يتوقف قبل
  «كتابة سجل الاستخراج» — مقيس حيًّا في 2026-09-10 (بقيت المحاولات 2 بعد فشل الرصيد).
- عقدة جديدة «تجديد القفل» بعد «كتابة سجل الاستخراج»: تكتب وقتًا جديدًا في عمود وقت القفل بعد
  كل ملف، فلا يمكن لمعالجة طويلة أن تتجاوز مهلة الثلاثين دقيقة فتسمح بتشغيل متوازٍ.
  (`onError: continueRegularOutput` حتى لا يُفشل التجديد تشغيلًا ناجحًا.)
- اختبارات هذه القواعد في `n8n/rules.test.js` — 14 تأكيدًا، تُشغَّل بـ `node n8n/rules.test.js`.

### bc81c99f — «مهلة القفل 30 دقيقة + تحريره عند الفشل»
- «هل القفل لي؟»: `TTL_MIN` من 20 إلى **30** دقيقة. أي صف قفل أقدم من ذلك يُهمَل فلا يعطّل
  التشغيلات، فلا يمكن لقفل عالق أن يوقف النظام أكثر من نصف ساعة.
- workflow التنبيه «تنبيه فشل التشغيل» (0INQLk2oWfw6WAEw) صار 5 عقد:
  `Error Trigger` يتفرّع إلى (أ) `Write Failure Row` كما كان، و(ب) `قراءة القفل (فشل)` →
  `صف القفل لهذه التشغيلة` (Code يجد الصف الذي معرّفه = معرّف التشغيلة الفاشلة) →
  `تحرير القفل (فشل)` الذي يمسح H:J لذلك الصف فورًا.
- نص التنبيه صار يترجم خطأ الرصيد: أي رسالة فيها `credit balance is too low` تُكتب
  «رصيد Anthropic API انتهى — …».
- ملاحظة سلوكية من n8n: الـerror workflow يُستدعى للتشغيلات الإنتاجية (المجدولة) فقط،
  لا للتشغيلات اليدوية — ولذلك بقي قفل التشغيلة اليدوية 762 عالقًا حتى نظّفناه، ومهلة
  الثلاثين دقيقة هي الضمانة في تلك الحالة.

### d36a1c88 — «حفظ جانب سماك عند إعادة المعالجة + ثقة medium مؤكدة من سماك»
- «Build Orders Table»: القراءة الجديدة تحلّ محلّ **جانب الفيديو فقط**. خلايا سماك
  (رقم الفاتورة، المنصة، الفرع، المنتجات، الإجمالي، طريقة الدفع، الخصم) تُنقل من الصف
  القديم عبر مرجع `carried`، وتُستخدم في المقارنة، ولا تُمسح إلا إذا وُجدت الفاتورة فعلًا في
  تقرير سماك الحالي؛ وإلا يُضاف للسبب «فاتورة سماك من بيانات الشيت — التقرير غير موجود».
  السبب الجذري: كل ملف موجود في مجلد الفيديو يُعاد إصداره كل تشغيل كأمر جديد (حتى بصفر
  استدعاءات خدمة)، فكان الصف المطابَق يفقد فاتورته في كل تشغيل إن كان تقريرها غير محمَّل.
- «فحص ذاتي للصف»: `applyMediumConfidence` — ثقة الإجمالي medium + سقوط هذا الفحص وحده +
  حالة ✅ + تطابق عمود الفيديو مع عمود سماك ⇒ ✅ مع «ثقة متوسطة — مؤكد من سماك».
  low تبقى «قراءة ضعيفة» دائمًا، ولا يُنشأ ⚠️ بثقة أقل من high.

### ffbfc69a — «قواعد سطري المنتج: لا دمج ولا ضرب في n8n»
- «Parse Video Result»: حُذف دمج الأصناف بالكود وحساب valA/valB — السعر الكلي يأتي جاهزًا من
  الخدمة (كوميت a65d18f ثم 286d7b6)، وتُمرَّر adjustments/items_count/merge_notes/subtotal/delivery.
- «Build Orders Table»: thechefz بطول 9، ونقل «مبلغ الخصم» من فاتورة سماك إلى السبب.
- «فحص ذاتي للصف»: thechefz 9، وأي خصم/كوبون يجعل الصف «يحتاج مراجعتي» مع ذكر مبلغه ونتيجة
  المطابقة.

### e47c24c1 — «استثناء بند المجموع الكلي + تأكيد إجمالي هنجر + كود الملصق»
- «Split Invoices & Items (Samak)»: البند يشترط رمز صنف أو اسم صنف (`iname == null`)، فلا
  يُقرأ صف «المجموع الكلى» كبند.
- «Build Orders Table»: `realItem` = كود أو اسم؛ المبلغ وحده لا يكفي (يمنع رجوع بند الشبح من
  نص الشيت).
- «فحص ذاتي للصف»: `applyTotalCorroboration` — عند غياب إجمالي مطبوع وتساوي مجموع البنود مع
  صافي سماك تمامًا تُرفع فحوص الإجمالي وحدها.
- «Parse Video Result»: اعتماد كود R من ملصق العبوة عند غياب الكود على شاشة التطبيق.
